create table USERLICENSE
(
   COMPUTERID int not null,
   USERID INT NOT NULL,
   APPID INT NOT NULL,
   COMPUTERTYPE varchar (30) not null,
   COMMENTS varchar (30) not null,
   PRIMARY KEY (COMPUTERID)
);